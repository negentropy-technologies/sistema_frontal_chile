# --- Google Earth Engine (GEE_PROJECT, GEE_USER) ---
earthengine-api
geemap          # timelapse/GIF export, timestamp overlays on frames

# --- NASA Earthdata (EARTHDATA_USER, EARTHDATA_PASSWORD) — GPM IMERG, MODIS ---
earthaccess

# --- Postgres/PostGIS + Martin tile server (DATABASE_URL, DB_*, MARTIN_DB_*) ---
psycopg2-binary
sqlalchemy
geoalchemy2

# --- SSH tunnel to remote DB (SSH_HOST, SSH_KEY_PATH, SSH_REMOTE_DB_PORT) ---
sshtunnel
paramiko<3.0  # sshtunnel 0.4.0 (latest) still references paramiko.DSSKey, removed in paramiko>=3

# --- Hugging Face (HF_TOKEN) ---
huggingface_hub

# --- general geo/data handling ---
python-dotenv
requests
pandas
geopandas
shapely
rasterio
Pillow